//when submitted, adds a new list to
// the user's collection with the data
// entered (includes smaller editing components)
const NewListForm = () => {
  //name

  //type - controlled by who called (listType prop)

  //ingredient editors- unknown number of them with '+' button

  //should I use a form for this?  Only validation would be the quantity entered
  //  could be overkill, but maybe cleaner than handling state
};

export default NewListForm;